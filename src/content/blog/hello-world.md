---
title: 'Hello, world'
description: 'A placeholder post to validate the blog collection, routes, and styling. Not real content.'
pubDate: 2026-08-07
draft: true
tags: ['meta', 'astro']
---

This is a **placeholder** stub used only to test the blog engine — the collection,
routes, RSS, and prose styling. Real posts land later.

## Prose renders in Sentient

Body copy uses the site's serif reading voice, with [an in-prose link](/contact) and
`inline code` styled to match the design system.

## Code blocks render in Commit Mono

```ts
// A fenced block — Commit Mono on a hairline --surface panel.
export function greet(name: string): string {
	return `hello, ${name}`;
}
```

- A list item
- Another item

> A short blockquote to confirm quote styling stays in the serif voice.
